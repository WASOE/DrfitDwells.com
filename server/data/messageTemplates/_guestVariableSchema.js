'use strict';

/**
 * Locked V1 guest-template variable schema (8 runtime variables).
 * Approved copy pack — see gmaApprovedCopy.js / GMA_bilingual_message_copy_for_Cursor.md.
 */
const { GUEST_TEMPLATE_VARIABLE_SCHEMA } = require('./gmaApprovedCopy');

module.exports = { GUEST_TEMPLATE_VARIABLE_SCHEMA };
