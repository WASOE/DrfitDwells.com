const CARD_OCCASIONS = ['birthday', 'anniversary', 'thank_you', 'wedding', 'last_minute', 'custom'];

const CARD_TEMPLATE_IDS = ['forest', 'romantic', 'minimal'];

const CARD_LOCALES = ['en', 'bg'];

const DELIVERY_OPTIONS = ['recipient_now', 'send_to_buyer', 'scheduled', 'postal'];

const MESSAGE_MAX_LENGTH = 250;

const SCHEDULED_DELIVERY_ENV_FLAG = 'GIFT_VOUCHER_SCHEDULED_ENABLED';

module.exports = {
  CARD_OCCASIONS,
  CARD_TEMPLATE_IDS,
  CARD_LOCALES,
  DELIVERY_OPTIONS,
  MESSAGE_MAX_LENGTH,
  SCHEDULED_DELIVERY_ENV_FLAG
};
